"""Generator package.

The canonical registries live in :mod:`registry` (``CLASS_REGISTRY`` /
``PROBLEM_REGISTRY``) and the orchestration in :mod:`engine`. All topic
generators are class-based under :mod:`topics`. The old function-based registry
that used to live here was removed once every topic was ported (see
GENERATOR_REWORK.md P4.5).
"""
